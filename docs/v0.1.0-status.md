# ALP v0.1.0 Status

## Completed in this branch

- Service provider bindings and publishable config setup.
- `alp:install` command scaffold for package installation.
- Upload request/controller scaffolding.
- Document ingestion service with normalization and storage orchestration.
- Apryse client contract and implementation stub.
- Text extraction and metadata extraction services.
- Core events: uploaded, normalized, processed, failed, metadata extracted.

## Remaining to fully close v0.1.0

- Full Laravel app integration (HTTP kernel, policies, real auth/rate limits).
- Persistent repository implementation backed by database.
- Queue worker and listener wiring in a running application context.
- Rich Apryse SDK integration beyond stubbed extraction behavior.
