# ALP v0.2.0 Status

## Implemented in this branch

- Table detection service with normalized cell output payload.
- Layout parser strategy and layout parsing service.
- AI provider abstraction (`AiProviderInterface`) and manager (`AiManager`).
- Local AI provider for deterministic summarization, entity extraction, and Q&A.
- AI services:
  - `DocumentSummarizationService`
  - `EntityExtractionService`
  - `DocumentQaService`
- Structured document store repository, service, and migration.
- Named pipeline execution support through `PipelineManager` and `PipelineService`.
- Provider-level DI bindings and configuration for v0.2 services.
- Unit coverage for v0.2 table/layout, AI, and pipeline flows.

## Remaining for v0.2 completion

- Real Apryse table/layout adapters and artifact persistence.
- Production-grade AI providers and prompt versioning.
- Queue jobs for heavy table detection and summarization workloads.
- HTTP/API endpoints for querying structured documents.
